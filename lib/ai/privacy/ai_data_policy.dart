class AiDataPolicy {
  static const allowedDataDescription =
      'Bizoot local AI uses only service names, amounts, categories, billing frequency, and dates.';

  static const blockedDataDescription =
      'Bizoot does not include passwords, login details, phone numbers, full names, emails, or direct personal identifiers in future cloud AI payloads.';
}
